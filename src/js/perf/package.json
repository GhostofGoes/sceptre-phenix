{
  "name": "phenix-ui-perf",
  "private": true,
  "description": "Bundle size budgets and Lighthouse audits for the phenix web UI",
  "scripts": {
    "size": "size-limit",
    "lighthouse": "lhci autorun"
  },
  "devDependencies": {
    "@lhci/cli": "0.15.1",
    "@size-limit/file": "14.0.0",
    "size-limit": "14.0.0"
  }
}
